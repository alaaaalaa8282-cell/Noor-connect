package com.noorconnect.app;

public class PrayerWidgetLarge extends PrayerWidget {
    // Large (4×4) full prayer timetable widget.
    // All logic lives in PrayerWidget.updateAppWidget() — dispatched by layout
    // type.
}
